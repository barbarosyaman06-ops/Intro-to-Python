fruits=["grape","orange","aplle","kiwi","pomegrante","pear","fig","apricot"]
new_list=[item for item in fruits if(item != "apricot")]
newets_list=[items.upper() for items in fruits]


# i=0
# while(i<len(fruits)):
#     print(fruits[i])
#     i+=1



#the summary is that we can use the len ready function to somewhat convert the string
#based list to a integerized range function whicl led us to use both i and [] index
#methods

#list comperhaension shortens the rows used for code and ease the operation so
#it is useful


# #   this is the first way:
# for item in (fruits):
#       if("g" in item):
#            new_list.append(item)
print(new_list)
print(newets_list)

#[print(item) for item in fruits]

#this is the second and easier way

